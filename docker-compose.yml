version: '3.1'

services:
  mariadb:
    image: mariadb:latest
    container_name: TP_MVC_ASPNET_DB
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: mvc
    ports:
      - "3306:3306"
    networks:
      - mariadb_network

networks:
  mariadb_network:
    driver: bridge
